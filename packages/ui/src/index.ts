/**
 * @tower/ui - TowerOS UI Component Library
 *
 * Production-ready components implementing the TowerOS design system.
 * Provides both web (React) and native (React Native) components.
 *
 * Usage:
 * ```tsx
 * // Web components
 * import { Button, Input, Card } from '@tower/ui/web';
 *
 * // Native components
 * import { Button, Input, Card } from '@tower/ui/native';
 * ```
 */

export const version = '0.1.0';
